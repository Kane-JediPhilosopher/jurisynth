[pytest]
pythonpath =
    jurisynth/kg_construction_pipeline/src
markers =
    integration: test that loads persisted pilot artifacts or an external integration.
    live_nim: opt-in test that makes a bounded request to NVIDIA NIM.
